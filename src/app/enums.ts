export enum LocalStorage {
    ItensSaved = "itensSaved"
}

export enum AffirmationMessages {
    SAVE_SUCCESS = "Dados salvos com sucesso."
}

export enum ConfirmationMessages {
    EXCLUDE = "A exclusão de dados é irreversível, deseja continuar ?"
}

export enum ActionClickName {
    EXCLUDE = "delete",
    EDIT = "edit"
}